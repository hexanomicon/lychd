# Goal: Test the actual CLI command as a user would.
# download lychd
# spawn CLI command init
# files and dirs created


# Method: Use Click's CliRunner. Invoke lychd bind.
# Quadlets get transcribed from provided config


# Services are running


# Scope: Does the entire chain work? (Settings -> Scriber -> Filesystem -> Systemd reload).

# Location: In your structure, these live in tests/integration/ or a new tests/e2e/ folder.
