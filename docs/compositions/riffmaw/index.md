---
title: Riffmaw
icon: material/music-circle
---

# :material-music-circle: Riffmaw

A remembered drop, a guitar take, a sung line, or an empty session can all begin the same craft. Riffmaw makes their contributions attributable while the Magus composes, performs, selects, edits, and produces the musical work. No model, DAW, or generation method owns that judgment.

## Ways of making

| Road | Pattern | What can finish here |
| --- | --- | --- |
| **Forge** | `riffmaw.forge_track@2` | Offline composition/generation from a brief, references, lyrics, or empty session: `MusicCandidateSet@1` or `MusicFindingSet@1`. |
| **Perform** | `riffmaw.capture_take@2` | Explicitly armed audio/MIDI from played, sung, rapped, musical-spoken-word, or programmed performance: `PerformanceTake@1` and observations/findings. |
| **Jam** | `riffmaw.open_jam@2` | Performers and admitted machine players answering against mapped clocks: recoverable `MusicalPerformanceSession@1` or findings. |
| **Produce** | `riffmaw.forge_music_bundle@2` | Bounded arrangement, mix, master, probes and listening review: `MusicAssetBundle@1`, candidates, findings, or refusal. |

Each road settles its own Invocation. Exact artifacts can enter later forward work; one Pattern never nests or silently resumes another. A future Scroll may combine separately named capture/generation/render/review placements under its own revision, Resolution Lock, and authority ceiling. File access does not arm an input; a take does not admit training; a candidate mix is not a master approval; export does not publish.

## The work

[Sources](sources.md) finds the exact musical moment. [Sessions](sessions.md) preserves takes and shared time; [Music](music.md) develops them into an arrangement and master candidate. [Studio](studio.md) keeps tool roles and dated candidates separate from the craft. [Hardware](hardware.md) admits exact musical capture routes. [Voice](voice.md) and [Sound](sound.md) explain role boundaries; [Sync](sync.md) offers musical cues; [Package](package.md) seals the handoff.

## Reference contract

`riffmaw.music` revision `2` accepts a music brief plus explicitly admitted references, lyrics, takes, MIDI, scores, samples, or live inputs. Alongside the road-specific returns, it can issue `MusicCueMap@1`. It stops before ordinary dialogue, spoken replacement/localization, picture sound, visuals, final audiovisual assembly, impersonation, and publication. Language Edition, Broadcast, Voidlight, and their technical providers retain those offices.

Prompt, lyric, requested sung language, and interface locale remain separate. Exact model profiles must prove their musical/language behavior; a local ComfyUI or other Soulstone binding and a Portal API are independently evidenced implementations. [Music](music.md#language-belongs-to-the-musical-request) owns the request; [Studio](studio.md#current-candidate-map) records candidates.

## Revision continuity

Designed `riffmaw.audio` revision `1`, its `Sonic*` family, and former `riffmaw.forge_track@1`, `riffmaw.capture_take@1`, `riffmaw.open_jam@1`, `riffmaw.index_sources@1`, and `riffmaw.mark_moment@1` are retired without reinterpretation. New identity `riffmaw.music` revision `1` used the five Pattern names at `@2`. Revision `2` advances principal `riffmaw.forge_music_bundle` to `@2` for model-native pre-execution music authority and semantic admission. `MusicAssetBundle@1` already accommodates those exact provenance receipts; other music Patterns remain unchanged. No registry, Run, or stored application record used the retired designs, so no executable migration exists.

[Audio](../../adr/37-audio.md) · [Workflow](../../adr/28-workflow.md) · [Composition Portfolio](../index.md)
