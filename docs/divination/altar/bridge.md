---
title: Bridge
icon: material/bridge
---

# :material-bridge: Bridge: The Conversational Threshold

**Purpose.** The **Bridge** is the Altar instrument where the Magus offers bounded Intent,
converses with the running body, answers a supported consent request, and receives results. It is
also the connective tissue between the other instruments. A future typed-reference flow may bring
an exact object witnessed elsewhere back into a new question; that flow is not delivered yet.

**Current boundary.** The Svelte Bridge supports local sessions, a New Séance action, text message
submission, process-local run streaming, pending consent cards and decisions, session inspection,
closed server-validated GenUI fragments, and a compact per-turn link into selected-run evidence.
Completed Pydantic AI message suffixes provide bounded conversational continuity; the active
capability is woven only after Dispatcher grants it. It does not provide a general workflow authoring
assistant, cross-process event durability, a general run dashboard, arbitrary annotations, or an
ambient view of every Agent. [State owns the exact Bridge
boundary](../../state-of-the-work.md#bridge-surface).

**Law.** The Bridge carries Intent and projects replies; it does not become Weaver, Oculus,
artifact custody, or the owning service behind a referenced object. Litestar validates every request,
reference, descriptor, and decision. A conversational suggestion is never executable authority.

> _“The Bridge does not contain the country. It gives one named crossing and remembers which shore
> received the traveler.”_

!!! warning "Use the contained Altar"
    The Bridge shares the Altar's temporary same-host browser boundary. Keep the listener on
    literal `127.0.0.1`, use the dedicated local browser profile, and do not proxy, tunnel, or
    port-forward it. Begin with the [Altar boundary](./index.md) if that containment is not already
    in place.

## Conversation, Not a Universal Canvas

Bridge remains a reading and writing surface. It may summarize related work beneath a turn, but it
must not place the complete [Loom](./loom.md) or [Orb](./orb.md) canvas behind the
conversation. The operator should be able to answer one question, inspect one result, or follow one
Run without ambient motion competing with the text.

A compact **run strip** attaches exact identity to the turn that admitted it. The current strip
shows Run identity, Pattern revision, canonical status/activity, and an Orb link. The Orb owns
capture limits and retained evidence. Freshness and a structured **Why waiting?** explanation are
not yet shown in Bridge. A spinner, elapsed time, or open connection may not be promoted into
workflow progress.

## Designed: Pin and Ask

From Loom, the Orb, Nexus, or a contextual artifact reference, the Magus may eventually choose
**Pin and Ask**.
The pin is not copied prose or a canvas coordinate. It is a typed reference to an exact authorized
subject such as a Pattern revision and node, run and event, Animator observation, artifact
revision, or configuration proposal.

Before submission, the composer must preview:

- which references will be attached;
- which immutable revision or event each one names;
- what content is included, summarized, unavailable, or redacted;
- which authority permits its use; and
- which reference has expired or become inaccessible.

The Vessel reauthorizes the reference when the new Intent is admitted. A pin grants neither
retrieval nor mutation by itself.

## Completed conversational continuity

Bridge display turns and model history are deliberately different records. The current user turn
may appear immediately in the conversation, but only a successfully settled agent turn appends
Pydantic AI's completed message suffix to the session-owned model history. Provider-level hops
within that suffix are normalized to the owning LychD run, so a consent tool return can never be
retained without its originating call. A later run reads that completed history, retains newest
whole LychD turns within the configured turn and character budgets, and validates it back into
Pydantic messages. It then reassembles the Stable Floor under the actual grant so the environment
names the capability and resolved context window in use. On consent resume, the required current
chain is reserved whole while older settled turns are re-bounded under the newly acquired grant.

This proves bounded continuity over completed local session history. It does not claim memory
retrieval, full-session replay, concurrent-turn serializability, or Archive/Karma delivery.

## Designed: Propose in Loom

Natural language can help author work without becoming the work's authority. A future **Propose in
Loom** action may turn a bounded conversation into an attributable **charcoal** candidate against
an exact base Pattern revision. The proposal must preview its source session and turns, referenced
objects, assumptions, typed changes, validation result, and required review.

The proposal does not edit a published Pattern, invoke a run, or publish itself. It opens as an
inert candidate in Loom, where Weaver's future draft contract can validate and review it. Until
that declarative contract exists, Bridge must not imply that prose can be round-tripped into an
executable workflow.

## Results Return Through Their Owners

A Bridge reply may eventually carry an artifact reference, evaluation, or correlation:

- a retained artifact reference stays contextual until an authorized custody lifecycle exists;
- a Pattern reference opens in Loom;
- a Run or event opens in the Orb; and
- a correlated transition opens in [Nexus](./nexus.md).

The current Bridge provides the Run-to-Orb link; it does not yet implement this general return
flow. Any future return to Bridge creates a new bounded Intent with previewed references. It never silently
injects artifact bytes, trace content, secret values, or an entire conversation into Context.

For the instrument that exists now, enter:

```text
http://127.0.0.1:7134/bridge
```

> _Offer one bounded Intent. Let every later question carry the identity of what it asks about._
