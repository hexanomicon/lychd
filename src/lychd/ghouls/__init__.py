"""Ghouls — SAQ background workers (ADR 14).

The Vessel keeps the Brain; the Ghouls are the Hands. Background Rites are
registered here and dispatched onto the ``rites`` queue configured in
``lychd.config.components``.
"""
