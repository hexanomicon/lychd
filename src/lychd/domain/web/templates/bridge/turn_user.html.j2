{# A settled user turn. `turn` is a BridgeTurn. #}
<article class="turn turn--user" data-fragment="bridge.turn" data-state="settled" data-role="user">
  <div class="turn__body">{{ turn.content }}</div>
</article>
